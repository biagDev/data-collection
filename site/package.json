{
  "name": "data-collection-dashboard",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Static dashboard for the trading-stats data-collection repo. Read-only over analyses/**.",
  "scripts": {
    "data": "node scripts/build-data.mjs",
    "validate": "node scripts/validate-data.mjs",
    "dev": "npm run data && astro dev",
    "build": "npm run validate && npm run data && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}

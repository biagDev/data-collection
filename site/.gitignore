# Astro / Node build artifacts — never committed.
node_modules/
dist/
.astro/

# Generated data files — emitted by scripts/build-data.mjs at build time.
# The SOURCE data lives in ../analyses/** and is never touched.
src/data/*.json

.DS_Store
